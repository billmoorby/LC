# Time Complexity: O(n)

* Time Complexity is O(n) since we make a single pass over the 'height' array to compute the trapped water at each position.


# Space Complexity: O(1)

* Space Complexity is O(1) since we aren't creating any additional data structures, just some extra variables for the pointers.
